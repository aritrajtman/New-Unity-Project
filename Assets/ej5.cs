using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ej5 : MonoBehaviour {

    // Use this for initialization
    public int num1;
    public int num2;
	void Start () {
		if (num2 = 0)
            Debug.Log("no se puede divir por 0")
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
